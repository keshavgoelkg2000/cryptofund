import {
  InputSelectionUI,
  emailIcon,
  useScreenContext
} from "./chunk-Q64AMAXC.js";
import {
  Button,
  Container,
  Input,
  Line,
  ModalHeader,
  ModalTitle,
  Spacer,
  Spinner,
  Text,
  TextDivider,
  WalletEntryButton,
  fontSize,
  iconSize,
  media,
  newStyled,
  reservedScreens,
  spacing,
  useConnectionStatus,
  useCreateWalletInstance,
  useSetConnectedWallet,
  useSetConnectionStatus,
  useTheme,
  useWalletContext
} from "./chunk-MJO5JNCB.js";
import {
  PaperWallet
} from "./chunk-NMNJ7A2O.js";
import {
  require_jsx_runtime
} from "./chunk-3FPM5NHH.js";
import {
  require_react
} from "./chunk-U2VBBHZY.js";
import {
  init_shim
} from "./chunk-6SWLQQ3Q.js";
import {
  __toESM
} from "./chunk-GN3OLCG2.js";

// node_modules/@thirdweb-dev/react/dist/paperWallet-cec67362.browser.esm.js
init_shim();
var import_react = __toESM(require_react());
var import_jsx_runtime = __toESM(require_jsx_runtime());
var GoogleIcon = (props) => {
  return (0, import_jsx_runtime.jsxs)("svg", {
    xmlns: "http://www.w3.org/2000/svg",
    height: props.size,
    viewBox: "0 0 24 24",
    width: props.size,
    children: [(0, import_jsx_runtime.jsx)("path", {
      d: "M22.56 12.25c0-.78-.07-1.53-.2-2.25H12v4.26h5.92c-.26 1.37-1.04 2.53-2.21 3.31v2.77h3.57c2.08-1.92 3.28-4.74 3.28-8.09z",
      fill: "#4285F4"
    }), (0, import_jsx_runtime.jsx)("path", {
      d: "M12 23c2.97 0 5.46-.98 7.28-2.66l-3.57-2.77c-.98.66-2.23 1.06-3.71 1.06-2.86 0-5.29-1.93-6.16-4.53H2.18v2.84C3.99 20.53 7.7 23 12 23z",
      fill: "#34A853"
    }), (0, import_jsx_runtime.jsx)("path", {
      d: "M5.84 14.09c-.22-.66-.35-1.36-.35-2.09s.13-1.43.35-2.09V7.07H2.18C1.43 8.55 1 10.22 1 12s.43 3.45 1.18 4.93l2.85-2.22.81-.62z",
      fill: "#FBBC05"
    }), (0, import_jsx_runtime.jsx)("path", {
      d: "M12 5.38c1.62 0 3.06.56 4.21 1.64l3.15-3.15C17.45 2.09 14.97 1 12 1 7.7 1 3.99 3.47 2.18 7.07l3.66 2.84c.87-2.6 3.3-4.53 6.16-4.53z",
      fill: "#EA4335"
    }), (0, import_jsx_runtime.jsx)("path", {
      d: "M1 1h22v22H1z",
      fill: "none"
    })]
  });
};
function openGoogleSignInWindow(themeObj) {
  const width = 350;
  const height = 500;
  const top = (window.innerHeight - height) / 2;
  const left = (window.innerWidth - width) / 2;
  const win = window.open("", void 0, `width=${width}, height=${height}, top=${top}, left=${left}`);
  if (win) {
    win.document.title = "Sign In - Google Accounts";
    win.document.body.innerHTML = spinnerWindowHtml;
    win.document.body.style.background = themeObj.colors.modalBg;
    win.document.body.style.color = themeObj.colors.accentText;
  }
  if (win) {
    window.addEventListener("beforeunload", () => {
      win == null ? void 0 : win.close();
    });
  }
  return win;
}
var spinnerWindowHtml = `
<svg class="loader" viewBox="0 0 50 50">
  <circle
    cx="25"
    cy="25"
    r="20"
    fill="none"
    stroke="currentColor"
    stroke-width="4"
  />
</svg>

<style>
  body,
  html {
    height: 100%;
    margin: 0;
    padding: 0;
  }

  body {
    display: flex;
    justify-content: center;
    align-items: center;
  }

  .loader {
    width: 15vw;
    height: 15vw;
    animation: spin 2s linear infinite;
  }

  .loader circle {
    animation: loading 1.5s linear infinite;
  }

  @keyframes loading {
    0% {
      stroke-dasharray: 1, 150;
      stroke-dashoffset: 0;
    }
    50% {
      stroke-dasharray: 90, 150;
      stroke-dashoffset: -35;
    }
    100% {
      stroke-dasharray: 90, 150;
      stroke-dashoffset: -124;
    }
  }

  @keyframes spin {
    100% {
      transform: rotate(360deg);
    }
  }
</style>
`;
function OTPInput(props) {
  const otp = props.value.split("").map(Number);
  const setOTP = (newOTP) => {
    props.setValue(newOTP.join(""));
  };
  const inputToFocusIndex = otp.length;
  const boxEls = (0, import_react.useRef)([]);
  (0, import_react.useEffect)(() => {
    if (boxEls.current[inputToFocusIndex]) {
      requestAnimationFrame(() => {
        var _a;
        (_a = boxEls.current[inputToFocusIndex]) == null ? void 0 : _a.focus();
      });
    }
  }, [inputToFocusIndex]);
  return (0, import_jsx_runtime.jsx)(Container, {
    center: "x",
    gap: "sm",
    flex: "row",
    children: new Array(props.digits).fill(null).map((_, i) => {
      var _a;
      return (0, import_jsx_runtime.jsx)(OTPInputBox, {
        "data-error": props.isInvalid,
        ref: (e) => boxEls.current[i] = e,
        value: (_a = otp[i]) != null ? _a : "",
        type: "number",
        pattern: "[0-9]*",
        variant: "outline",
        inputMode: "numeric",
        onPaste: (e) => {
          const pastedData = e.clipboardData.getData("text/plain");
          const newOTP = pastedData.slice(0, props.digits).split("").filter((n) => /\d/.test(n)).map(Number);
          setOTP(newOTP);
          e.preventDefault();
        },
        onKeyDown: (e) => {
          var _a2, _b;
          if (e.key === "Enter") {
            if (props.onEnter) {
              props.onEnter();
              return;
            }
          }
          if (e.key === "ArrowLeft") {
            if (i === 0) {
              return;
            }
            (_a2 = boxEls.current[i - 1]) == null ? void 0 : _a2.focus();
            return;
          }
          if (e.key === "ArrowRight") {
            if (i === props.digits - 1) {
              return;
            }
            (_b = boxEls.current[i + 1]) == null ? void 0 : _b.focus();
            return;
          }
          if (e.key === "e" || e.key === ".") {
            e.preventDefault();
            return;
          }
          if (e.key === "Backspace") {
            if (i === 0) {
              return;
            }
            const newOTP = otp.slice(0, -1);
            setOTP(newOTP);
          }
        },
        onChange: (e) => {
          let value = e.target.value;
          if (value.length > 1) {
            const lastValue = value[value.length - 1];
            if (lastValue) {
              value = lastValue;
            }
          }
          if (!/\d/.test(value) && value !== "") {
            e.preventDefault();
            return;
          }
          const newOTP = [...otp];
          const index = i > inputToFocusIndex - 1 ? inputToFocusIndex : i;
          newOTP[index] = Number(value);
          setOTP(newOTP);
        }
      }, i);
    })
  });
}
var OTPInputBox = newStyled(Input)`
  appearance: none;
  -webkit-appearance: none;
  width: 40px;
  height: 40px;
  text-align: center;
  font-size: ${fontSize.md};
  padding: ${spacing.xs};
  ${media.mobile} {
    width: 35px;
    height: 35px;
  }
  &[data-verify-status="invalid"] {
    color: ${(p) => p.theme.colors.danger};
    border-color: ${(p) => p.theme.colors.danger};
  }
`;
var PaperFormUI = (props) => {
  const createWalletInstance = useCreateWalletInstance();
  const setConnectionStatus = useSetConnectionStatus();
  const setConnectedWallet = useSetConnectedWallet();
  const themeObj = useTheme();
  const googleLogin = async () => {
    try {
      const paperWallet2 = createWalletInstance(props.walletConfig);
      setConnectionStatus("connecting");
      const googleWindow = openGoogleSignInWindow(themeObj);
      if (!googleWindow) {
        throw new Error("Failed to open google login window");
      }
      await paperWallet2.connect({
        googleLogin: {
          openedWindow: googleWindow,
          closeOpenedWindow: (openedWindow) => {
            openedWindow.close();
          }
        }
      });
      setConnectedWallet(paperWallet2);
    } catch (e) {
      setConnectionStatus("disconnected");
      console.error(e);
    }
  };
  return (0, import_jsx_runtime.jsxs)("div", {
    children: [props.googleLoginSupported && (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, {
      children: [(0, import_jsx_runtime.jsxs)(SocialButton, {
        variant: "secondary",
        fullWidth: true,
        onClick: () => {
          googleLogin();
          props.onSelect({
            google: true
          });
        },
        children: [(0, import_jsx_runtime.jsx)(GoogleIcon, {
          size: iconSize.md
        }), "Sign in with Google"]
      }), (0, import_jsx_runtime.jsx)(TextDivider, {
        text: "OR",
        py: "lg"
      })]
    }), (0, import_jsx_runtime.jsx)(InputSelectionUI, {
      onSelect: (email) => props.onSelect({
        email
      }),
      placeholder: "Enter your email address",
      name: "email",
      type: "email",
      errorMessage: (_input) => {
        const input = _input.replace(/\+/g, "");
        const emailRegex = /^([a-z0-9_\.-]+)@([\da-z\.-]+)\.([a-z\.]{2,})$/g;
        const isValidEmail = emailRegex.test(input);
        if (!isValidEmail) {
          return "Invalid email address";
        }
      },
      emptyErrorMessage: "email address is required"
    })]
  });
};
var PaperFormUIScreen = (props) => {
  const isCompact = props.modalSize === "compact";
  return (0, import_jsx_runtime.jsxs)(Container, {
    fullHeight: true,
    flex: "column",
    p: "lg",
    animate: "fadein",
    style: {
      minHeight: "250px"
    },
    children: [(0, import_jsx_runtime.jsx)(ModalHeader, {
      onBack: props.onBack,
      title: "Sign in"
    }), isCompact ? (0, import_jsx_runtime.jsx)(Spacer, {
      y: "xl"
    }) : null, (0, import_jsx_runtime.jsx)(Container, {
      expand: true,
      flex: "column",
      center: "y",
      p: isCompact ? void 0 : "lg",
      children: (0, import_jsx_runtime.jsx)(PaperFormUI, {
        walletConfig: props.walletConfig,
        googleLoginSupported: props.googleLoginSupported,
        onSelect: props.onSelect
      })
    })]
  });
};
var SocialButton = newStyled(Button)`
  display: flex;
  justify-content: center;
  gap: ${spacing.sm};
`;
var PaperOTPLoginUI = (props) => {
  const email = props.selectionData;
  const [otpInput, setOtpInput] = (0, import_react.useState)("");
  const [recoveryCode, setRecoveryCode] = (0, import_react.useState)("");
  const {
    createWalletInstance,
    setConnectedWallet,
    setConnectionStatus
  } = useWalletContext();
  const [wallet, setWallet] = (0, import_react.useState)(null);
  const isWideModal = props.modalSize === "wide";
  const [verifyStatus, setVerifyStatus] = (0, import_react.useState)("idle");
  const [sendEmailStatus, setSendEmailStatus] = (0, import_react.useState)("sending");
  const recoveryCodeRequired = !!(typeof sendEmailStatus !== "string" && sendEmailStatus.recoveryShareManagement !== "AWS_MANAGED" && sendEmailStatus.isNewDevice && !sendEmailStatus.isNewUser);
  const sendEmail = (0, import_react.useCallback)(async () => {
    setOtpInput("");
    setVerifyStatus("idle");
    setSendEmailStatus("sending");
    try {
      const _wallet = createWalletInstance(props.walletConfig);
      setWallet(_wallet);
      const _paperSDK = await _wallet.getPaperSDK();
      const {
        isNewDevice,
        isNewUser,
        recoveryShareManagement
      } = await _paperSDK.auth.sendPaperEmailLoginOtp({
        email
      });
      setSendEmailStatus({
        isNewDevice,
        isNewUser,
        recoveryShareManagement
      });
    } catch (e) {
      console.error(e);
      setVerifyStatus("idle");
      setSendEmailStatus("error");
    }
  }, [createWalletInstance, email, props.walletConfig]);
  const handleSubmit = (otp) => {
    if (recoveryCodeRequired && !recoveryCode) {
      return;
    }
    if (typeof sendEmailStatus === "string" || otp.length !== 6) {
      return;
    }
    verifyCodes(otp);
  };
  const verifyCodes = async (otp) => {
    setVerifyStatus("idle");
    if (!wallet) {
      return;
    }
    try {
      setVerifyStatus("verifying");
      setConnectionStatus("connecting");
      await wallet.connect({
        email,
        otp,
        recoveryCode: recoveryCodeRequired ? recoveryCode : void 0
      });
      setConnectedWallet(wallet);
      setVerifyStatus("valid");
      props.connected();
    } catch (e) {
      setVerifyStatus("invalid");
      console.error(e);
    }
  };
  const emailSentOnMount = (0, import_react.useRef)(false);
  (0, import_react.useEffect)(() => {
    if (!emailSentOnMount.current) {
      emailSentOnMount.current = true;
      sendEmail();
    }
  }, [sendEmail]);
  return (0, import_jsx_runtime.jsxs)(Container, {
    fullHeight: true,
    flex: "column",
    animate: "fadein",
    children: [(0, import_jsx_runtime.jsx)(Container, {
      p: "lg",
      children: (0, import_jsx_runtime.jsx)(ModalHeader, {
        title: "Sign in",
        onBack: props.goBack
      })
    }), (0, import_jsx_runtime.jsx)(Container, {
      expand: true,
      flex: "column",
      center: "y",
      children: (0, import_jsx_runtime.jsxs)("form", {
        onSubmit: (e) => {
          e.preventDefault();
        },
        children: [(0, import_jsx_runtime.jsxs)("div", {
          style: {
            textAlign: "center"
          },
          children: [!isWideModal && (0, import_jsx_runtime.jsx)(Spacer, {
            y: "lg"
          }), (0, import_jsx_runtime.jsx)(Text, {
            children: "Enter the verification code sent to"
          }), (0, import_jsx_runtime.jsx)(Spacer, {
            y: "sm"
          }), (0, import_jsx_runtime.jsx)(Text, {
            color: "primaryText",
            children: email
          }), (0, import_jsx_runtime.jsx)(Spacer, {
            y: "xl"
          })]
        }), (0, import_jsx_runtime.jsx)(OTPInput, {
          isInvalid: verifyStatus === "invalid",
          digits: 6,
          value: otpInput,
          setValue: (value) => {
            setOtpInput(value);
            setVerifyStatus("idle");
            if (!recoveryCodeRequired) {
              handleSubmit(value);
            }
          },
          onEnter: () => {
            handleSubmit(otpInput);
          }
        }), recoveryCodeRequired && (0, import_jsx_runtime.jsxs)(Container, {
          animate: "fadein",
          px: "lg",
          style: {
            textAlign: "center"
          },
          children: [(0, import_jsx_runtime.jsx)(Spacer, {
            y: "xxl"
          }), (0, import_jsx_runtime.jsx)(Text, {
            color: "primaryText",
            children: "New device detected"
          }), (0, import_jsx_runtime.jsx)(Spacer, {
            y: "sm"
          }), (0, import_jsx_runtime.jsxs)(Text, {
            size: "sm",
            multiline: true,
            style: {
              maxWidth: "350px"
            },
            children: ["Enter the recovery code emailed to you ", (0, import_jsx_runtime.jsx)("br", {}), " when you first signed up"]
          }), (0, import_jsx_runtime.jsx)(Spacer, {
            y: "lg"
          }), (0, import_jsx_runtime.jsx)(Input, {
            sm: true,
            autoComplete: "off",
            required: true,
            "data-error": verifyStatus === "invalid",
            id: "recovery-code",
            variant: "outline",
            style: {
              textAlign: "center"
            },
            value: recoveryCode,
            onChange: (e) => setRecoveryCode(e.target.value),
            placeholder: "Enter your recovery code"
          })]
        }), verifyStatus === "invalid" && (0, import_jsx_runtime.jsxs)(Container, {
          animate: "fadein",
          children: [(0, import_jsx_runtime.jsx)(Spacer, {
            y: "md"
          }), (0, import_jsx_runtime.jsxs)(Text, {
            size: "sm",
            color: "danger",
            center: true,
            children: ["Invalid verification code", " ", recoveryCodeRequired ? "or recovery code" : ""]
          })]
        }), (0, import_jsx_runtime.jsx)(Spacer, {
          y: "xl"
        }), (0, import_jsx_runtime.jsx)(Container, {
          px: isWideModal ? "xxl" : "lg",
          children: verifyStatus === "verifying" ? (0, import_jsx_runtime.jsx)(import_jsx_runtime.Fragment, {
            children: (0, import_jsx_runtime.jsx)(Container, {
              flex: "row",
              center: "x",
              animate: "fadein",
              children: (0, import_jsx_runtime.jsx)(Spinner, {
                size: "lg",
                color: "accentText"
              })
            })
          }) : (0, import_jsx_runtime.jsx)(Container, {
            animate: "fadein",
            children: (0, import_jsx_runtime.jsx)(Button, {
              onClick: () => handleSubmit(otpInput),
              variant: "accent",
              type: "submit",
              style: {
                width: "100%"
              },
              children: "Verify"
            })
          }, "btn-container")
        }), (0, import_jsx_runtime.jsx)(Spacer, {
          y: "xl"
        }), !isWideModal && (0, import_jsx_runtime.jsx)(Line, {}), (0, import_jsx_runtime.jsxs)(Container, {
          p: isWideModal ? void 0 : "lg",
          animate: "fadein",
          children: [sendEmailStatus === "error" && (0, import_jsx_runtime.jsx)(import_jsx_runtime.Fragment, {
            children: (0, import_jsx_runtime.jsx)(Text, {
              size: "sm",
              center: true,
              color: "danger",
              children: "Failed to send verification code"
            })
          }), sendEmailStatus === "sending" && (0, import_jsx_runtime.jsxs)(Container, {
            flex: "row",
            center: "both",
            gap: "xs",
            style: {
              textAlign: "center"
            },
            children: [(0, import_jsx_runtime.jsx)(Text, {
              size: "sm",
              children: "Sending verification code"
            }), (0, import_jsx_runtime.jsx)(Spinner, {
              size: "xs",
              color: "secondaryText"
            })]
          }), typeof sendEmailStatus !== "string" && (0, import_jsx_runtime.jsx)(LinkButton, {
            onClick: sendEmail,
            type: "button",
            children: "Resend verification code"
          })]
        })]
      })
    })]
  });
};
var LinkButton = newStyled.button`
  all: unset;
  color: ${(p) => p.theme.colors.accentText};
  font-size: ${fontSize.sm};
  cursor: pointer;
  text-align: center;
  width: 100%;
  &:hover {
    color: ${(p) => p.theme.colors.primaryText};
  }
`;
var PaperGoogleLogin = (props) => {
  const {
    goBack,
    modalSize,
    connected
  } = props;
  const createWalletInstance = useCreateWalletInstance();
  const setConnectionStatus = useSetConnectionStatus();
  const setConnectedWallet = useSetConnectedWallet();
  const connectionStatus = useConnectionStatus();
  const themeObj = useTheme();
  const googleLogin = async () => {
    try {
      const paperWallet2 = createWalletInstance(props.walletConfig);
      setConnectionStatus("connecting");
      const googleWindow = openGoogleSignInWindow(themeObj);
      if (!googleWindow) {
        throw new Error("Failed to open google login window");
      }
      await paperWallet2.connect({
        googleLogin: {
          openedWindow: googleWindow,
          closeOpenedWindow: (openedWindow) => {
            openedWindow.close();
          }
        }
      });
      setConnectedWallet(paperWallet2);
      props.connected();
    } catch (e) {
      setConnectionStatus("disconnected");
      console.error(e);
    }
  };
  (0, import_react.useEffect)(() => {
    if (connectionStatus === "connected") {
      connected();
    }
  }, [connectionStatus, connected]);
  return (0, import_jsx_runtime.jsx)(Container, {
    animate: "fadein",
    flex: "column",
    fullHeight: true,
    children: (0, import_jsx_runtime.jsxs)(Container, {
      flex: "column",
      expand: true,
      p: "lg",
      style: {
        paddingBottom: 0
      },
      children: [(0, import_jsx_runtime.jsx)(ModalHeader, {
        title: (0, import_jsx_runtime.jsxs)(Container, {
          flex: "row",
          center: "both",
          gap: "xs",
          children: [(0, import_jsx_runtime.jsx)(GoogleIcon, {
            size: iconSize.md
          }), (0, import_jsx_runtime.jsx)(ModalTitle, {
            children: " Sign in "
          })]
        }),
        onBack: goBack
      }), modalSize === "compact" ? (0, import_jsx_runtime.jsx)(Spacer, {
        y: "xl"
      }) : null, (0, import_jsx_runtime.jsxs)(Container, {
        flex: "column",
        center: "both",
        expand: true,
        style: {
          textAlign: "center",
          minHeight: "250px"
        },
        children: [connectionStatus === "connecting" && (0, import_jsx_runtime.jsxs)(Container, {
          animate: "fadein",
          children: [(0, import_jsx_runtime.jsx)(Text, {
            color: "primaryText",
            multiline: true,
            style: {
              maxWidth: "250px"
            },
            children: "Select your Google account in the pop-up"
          }), (0, import_jsx_runtime.jsx)(Spacer, {
            y: "xl"
          }), (0, import_jsx_runtime.jsx)(Container, {
            center: "x",
            flex: "row",
            children: (0, import_jsx_runtime.jsx)(Spinner, {
              size: "lg",
              color: "accentText"
            })
          }), (0, import_jsx_runtime.jsx)(Spacer, {
            y: "xxl"
          })]
        }), connectionStatus === "disconnected" && (0, import_jsx_runtime.jsxs)(Container, {
          animate: "fadein",
          children: [(0, import_jsx_runtime.jsx)(Text, {
            color: "danger",
            children: "Failed to sign in"
          }), (0, import_jsx_runtime.jsx)(Spacer, {
            y: "lg"
          }), (0, import_jsx_runtime.jsxs)(Button, {
            variant: "primary",
            onClick: googleLogin,
            children: [" ", "Retry", " "]
          }), (0, import_jsx_runtime.jsx)(Spacer, {
            y: "xxl"
          })]
        })]
      })]
    })
  });
};
var paperWallet = (config) => {
  const defaultRecovery = "AWS_MANAGED";
  return {
    category: "socialLogin",
    isHeadless: true,
    id: PaperWallet.id,
    recommended: config == null ? void 0 : config.recommended,
    meta: {
      ...PaperWallet.meta,
      name: "Email",
      iconURL: emailIcon
    },
    create(options) {
      return new PaperWallet({
        ...options,
        ...config,
        advancedOptions: {
          recoveryShareManagement: "AWS_MANAGED",
          ...config == null ? void 0 : config.advancedOptions
        }
      });
    },
    selectUI(props) {
      var _a;
      return (0, import_jsx_runtime.jsx)(PaperSelectionUI, {
        ...props,
        recoveryShareManagement: ((_a = config == null ? void 0 : config.advancedOptions) == null ? void 0 : _a.recoveryShareManagement) || defaultRecovery
      });
    },
    connectUI(props) {
      var _a;
      return (0, import_jsx_runtime.jsx)(PaperConnectUI, {
        ...props,
        recoveryShareManagement: ((_a = config == null ? void 0 : config.advancedOptions) == null ? void 0 : _a.recoveryShareManagement) || defaultRecovery
      });
    }
  };
};
var PaperSelectionUI = (props) => {
  const screen = useScreenContext();
  if (props.modalSize === "wide" || screen !== reservedScreens.main && props.modalSize === "compact") {
    return (0, import_jsx_runtime.jsx)(WalletEntryButton, {
      walletConfig: props.walletConfig,
      selectWallet: () => {
        props.onSelect(void 0);
      }
    });
  }
  return (0, import_jsx_runtime.jsx)("div", {
    children: (0, import_jsx_runtime.jsx)(PaperFormUI, {
      walletConfig: props.walletConfig,
      googleLoginSupported: props.recoveryShareManagement !== "USER_MANAGED",
      onSelect: props.onSelect
    })
  });
};
var PaperConnectUI = (props) => {
  const [loginType, setLoginType] = (0, import_react.useState)(props.selectionData);
  if (loginType) {
    const handleBack = () => {
      if (props.modalSize === "wide") {
        setLoginType(void 0);
      } else {
        props.goBack();
      }
    };
    if ("email" in loginType) {
      return (0, import_jsx_runtime.jsx)(PaperOTPLoginUI, {
        ...props,
        recoveryShareManagement: props.recoveryShareManagement,
        selectionData: loginType.email,
        goBack: handleBack
      });
    } else {
      return (0, import_jsx_runtime.jsx)(PaperGoogleLogin, {
        ...props,
        goBack: handleBack
      });
    }
  }
  return (0, import_jsx_runtime.jsx)(PaperFormUIScreen, {
    walletConfig: props.walletConfig,
    googleLoginSupported: props.recoveryShareManagement !== "USER_MANAGED",
    modalSize: props.modalSize,
    onSelect: (_loginType) => {
      setLoginType(_loginType);
    },
    onBack: props.goBack
  });
};
var paperWallet$1 = Object.freeze({
  __proto__: null,
  paperWallet
});

export {
  GoogleIcon,
  openGoogleSignInWindow,
  OTPInput,
  paperWallet,
  paperWallet$1
};
//# sourceMappingURL=chunk-JZVOVGZT.js.map
